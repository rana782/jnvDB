# Leave empty in local dev to use Vite proxy (/api -> http://127.0.0.1:4000).
# In production (for example on Vercel), set this to your API origin, e.g.:
# VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=
