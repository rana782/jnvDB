-- pgAdmin: connect to your server (e.g. jnvDB) → select database "postgres" → Query Tool → Execute.

CREATE DATABASE jnv_intel;

-- If the database already exists, pgAdmin will show an error — that is OK; keep using jnv_intel.
